---
layout: ../../../layouts/Layout.astro
pubDate: "2024-02-16"
updatedDate: "2024-02-16"
title: "React で <title> をいじる方法"
author: ダーシノ
description: "Reactで`<title>`を変更する方法。"
tags: ["TypeScript"]
---

import Header from '../../../components/Header.astro'

<Header {...frontmatter} />

Reactで`<title>`を変更する方法。

## Before

```tsx
export interface TitleProps {
  children: string
}
export function Title(props: TitleProps): JSX.Element {
  useEffect(() => {
    document.title = props.children
  }, [])
  return <></>
}

// 使い方
<Component>
  <Title>ここにタイトルを設定</Title>
</Component>
```

## After: React19で追加予定の`<title>`コンポーネント

- `<title>`コンポーネントが公式に追加される
- `document.title`をいじる必要がなくなる

```tsx
<Component>
  <title>ここにタイトルを設定</title>
</Component>

// ↓↓↓

<head>
  <title>ここにタイトルを設定</title>
</head>
<body>
  ...
</body>
```
