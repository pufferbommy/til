---
layout: ../../../layouts/Layout.astro
pubDate: "2024-04-26"
updatedDate: "2024-04-26"
title: "Popover API + CSS Anchor Positioningでツールチップを表示する"
author: ダーシノ
description: "Popover API + CSS Anchor Positioningでツールチップを表示する。"
tags: ["HTML", "CSS"]
---

import Header from '../../../components/Header.astro'
import './style.css'

<Header {...frontmatter} />

Popover API + CSS Anchor Positioningでツールチップを表示する。

```html
<form>
  <div>
    <label for="account">アカウント</label>
    <button
      type="button"
      id="anchor-account"
      popovertarget="tooltip-account"
      aria-label="アカウントの説明を表示する"
    >(?)</button>
    <p
      class="tooltip"
      id="tooltip-account"
      anchor="anchor-account"
      popover
    >← アカウントはあれが、それで、こうなる</p>
    <input id="account" type="text">
  </div>
  ...
</form>

<style>
.tooltip {
  /* 勝手にmarginがとられてしまうので無効にする */
  margin: 0;
  /* anchorに対してどの位置にtooltipを表示するか指定する。この他に top: anchor(center) のような指定の仕方もある。 */
  inset-area: top;
  border: 2px solid;
}
</style>
```

## デモ

<form>
  <div>
    <label for="account">アカウント</label>
    <button
      type="button"
      id="anchor-account"
      popovertarget="tooltip-account"
      aria-label="アカウントの説明を表示する"
    >(?)</button>
    <p
      class="tooltip"
      id="tooltip-account"
      anchor="anchor-account"
      popover
    >アカウントはあれが、それで、こうなる</p>
    <input id="account" type="text" />
  </div>
  <div>
    <label for="name">名前</label>
    <button
      type="button"
      id="anchor-name"
      popovertarget="tooltip-name"
      aria-label="名前の説明を表示する"
    >(?)</button>
    <p
      class="tooltip"
      id="tooltip-name"
      anchor="anchor-name"
      popover
    >名前はあれが、それで、こうなる</p>
    <input id="name" type="text" />
  </div>
</form>
